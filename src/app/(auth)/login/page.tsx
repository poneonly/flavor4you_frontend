import { LoginView } from 'features/auth';

export const metadata = {
  title: 'Login - Flavor4You',
  description: 'Login to your Flavor4You account',
};

export default function LoginPage() {
  return <LoginView />;
}

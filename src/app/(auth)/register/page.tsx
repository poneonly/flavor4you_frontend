import { RegisterView } from 'features/auth';

export const metadata = {
  title: 'Register - Flavor4You',
  description: 'Create a new Flavor4You account',
};

export default function RegisterPage() {
  return <RegisterView />;
}
